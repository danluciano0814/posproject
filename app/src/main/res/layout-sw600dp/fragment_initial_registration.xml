<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:padding="10dp"
    android:focusableInTouchMode="true">

    <TextView
        android:text="Initial Registration"
        android:id="@+id/textView2"
        style="@style/blue_title_txt"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:text="NOTE: Registration require internet and a valid email for password recovering purposes."/>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/transparent_gray_bg"
            android:orientation="vertical"
            android:padding="10dp">

            <TextView
                android:text="EMAIL"
                style="@style/blue_normal_txt"
                />

            <EditText
                android:inputType="textEmailAddress"
                android:id="@+id/etEmail"
                style="@style/edittext_form" />

            <TextView
                android:text="FIRST NAME"
                style="@style/blue_normal_txt"/>

            <EditText
                android:inputType="text"
                android:id="@+id/etFname"
                style="@style/edittext_form" />

            <TextView
                android:text="LAST NAME"
                style="@style/blue_normal_txt"/>

            <EditText
                android:inputType="text"
                android:id="@+id/etLname"
                style="@style/edittext_form" />

            <Button
                android:text="Send"
                android:id="@+id/btnSend"
                style="@style/blue_form_btn" />

            <Button
                android:text="Enter Code"
                android:id="@+id/btnEnterCode"
                android:layout_marginBottom="10dp"
                style="@style/blue_form_btn" />


        </LinearLayout>

    </ScrollView>

</LinearLayout>